#include<stdio.h>
#include<math.h>
int main()
{
    int n = 0;
    while (scanf("%d",&n) != EOF)
    {
        int sum = 0;
        for (int i = 0; i < n; i++)
        {
            sum += 2+3*i;
        }
        printf("%d",sum);
    }
    return 0;
}
