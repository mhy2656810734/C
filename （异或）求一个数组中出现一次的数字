#include<stdio.h>
//异或具有交换律
//0^a=a;可以将数组中的每个数都异或，如果只有一个的输出的就是那个数    3^3^5=3^5^3
int main()
{
int arr[]={1,2,3,4,5,1,2,3,4};
int i;
int ret=0;
int sz=sizeof(arr)/sizeof(arr[0]);
for(i=0;i<sz;i++)
{
ret=ret^arr[i];
}
printf("%d\n",ret);
return 0;
}
