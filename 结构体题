//有 n 个平面向量 (x1,y1)  ，牛牛把这几个向量相加并输出这个向量的值。
//输入描述：
//第一行输入一个正整数 n 
//后续 n 行每行输入两个正整数分别是 x 和 y。
//输出描述：
//输出所有的向量相加的结果

#include<stdio.h>
typedef struct class{
   int x1[100];
   int y1[100];
}class;
int main()
{
    class Class;
    int n=0;
    scanf("%d",&n);
    int i=0;
    for(i=0;i<n;i++)
    {
        scanf("%d %d",&Class.x1[i],&Class.y1[i]);
    }
    int x=0;
    int y=0;
    for(i=0;i<n;i++)
    {
        x+=Class.x1[i];
        y+=Class.y1[i];
    }
    printf("%d %d",x,y);
    return 0;
}
