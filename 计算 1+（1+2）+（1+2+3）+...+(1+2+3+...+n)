#include<stdio.h>
int main()
{
    //思路，从后往前加   n的累加 +  n-1的累加 +.....+2的累加+1
    int n;
    scanf("%d", &n);
    int sum = 0;
    while (n)
    {
        int i = 0;
        for (i = 1; i <= n; i++)
        {
            sum += i;
        }
        n--;
    }
    printf("%d", sum);
    return 0;
}
